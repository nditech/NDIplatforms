core: 7.x
api: 2

projects:
  hierarchical_select: { version: ~ }
  quicktabs: { version: ~ }
  better_exposed_filters: { version: ~ }
  i18n: { version: ~ }
  i18nviews: { version: ~ }
  variable: { version: ~ }

  # Added for ndi_dkan_additions exported nodes
  uuid_features:
    download:
      url: http://git.drupal.org/project/uuid_features.git
      branch: 7.x-1.x
      # TODO: Remove this pinning once `drush make-lock` can lock to specific commits.
      revision: '8f7f1c5e1'

  # See: https://nationaldemocraticinstitute.atlassian.net/browse/DKAN-82
  visualization_entity_maps:
    download:
      url: https://github.com/NuCivic/visualization_entity_maps.git
      branch: master
      # TODO: Remove this pinning once `drush make-lock` can lock to specific commits.
      revision: d324462
 
